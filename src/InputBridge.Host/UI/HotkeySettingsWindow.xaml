<Window x:Class="InputBridge.Host.UI.HotkeySettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Hotkey Settings" Height="250" Width="300"
        Style="{StaticResource DefaultWindowStyle}"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Hotkeys" FontSize="16" FontWeight="Bold" Margin="0,0,0,15" Foreground="{StaticResource BrushText}"/>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Switch to This PC:" VerticalAlignment="Center" Foreground="{StaticResource BrushTextDim}"/>
            <TextBox x:Name="TxtHostHotkey" Grid.Column="1" Background="{StaticResource BrushSurface}" Foreground="{StaticResource BrushText}" BorderThickness="0" Padding="5"/>
        </Grid>

        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Switch to Client:" VerticalAlignment="Center" Foreground="{StaticResource BrushTextDim}"/>
            <TextBox x:Name="TxtClientHotkey" Grid.Column="1" Background="{StaticResource BrushSurface}" Foreground="{StaticResource BrushText}" BorderThickness="0" Padding="5"/>
        </Grid>

        <Button Grid.Row="3" x:Name="BtnSave" Content="Save" Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Right" Padding="20,8" Margin="0,15,0,0" Click="BtnSave_Click"/>
    </Grid>
</Window>
