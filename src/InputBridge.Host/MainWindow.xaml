<Window x:Class="InputBridge.Host.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="InputBridge Host" Height="450" Width="400"
        Style="{StaticResource DefaultWindowStyle}"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Status Panel -->
        <Border Grid.Row="0" Background="{StaticResource BrushSurface}" CornerRadius="8" Padding="15" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="🔴 Waiting for Connection..." Foreground="{StaticResource BrushError}" FontWeight="Bold" FontSize="16" x:Name="TxtStatus" />
                <TextBlock Text="Latency: -- ms | Mode: LOCAL" Foreground="{StaticResource BrushTextDim}" Margin="0,5,0,0" x:Name="TxtDetails" />
            </StackPanel>
        </Border>

        <!-- Mode Switches -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            
            <Button Grid.Column="0" Style="{StaticResource PrimaryButtonStyle}" x:Name="BtnLocalMode" Height="60">
                <StackPanel>
                    <TextBlock Text="🏠 This PC" FontWeight="Bold" HorizontalAlignment="Center" />
                    <TextBlock x:Name="TxtLocalHotkeyDisplay" Text="Ctrl+Win+1" FontSize="12" Foreground="{StaticResource BrushTextDim}" HorizontalAlignment="Center"/>
                    <TextBlock Text="[ACTIVE]" FontSize="11" Foreground="{StaticResource BrushSuccess}" HorizontalAlignment="Center" Margin="0,2,0,0" x:Name="TxtLocalActive"/>
                </StackPanel>
            </Button>

            <Button Grid.Column="2" Style="{StaticResource PrimaryButtonStyle}" Background="{StaticResource BrushSurface}" x:Name="BtnRemoteMode" Height="60">
                <StackPanel>
                    <TextBlock Text="💻 Client" FontWeight="Bold" HorizontalAlignment="Center" />
                    <TextBlock x:Name="TxtRemoteHotkeyDisplay" Text="Ctrl+Win+2" FontSize="12" Foreground="{StaticResource BrushTextDim}" HorizontalAlignment="Center"/>
                    <TextBlock Text="[ switch ]" FontSize="11" Foreground="{StaticResource BrushTextDim}" HorizontalAlignment="Center" Margin="0,2,0,0" x:Name="TxtRemoteActive"/>
                </StackPanel>
            </Button>
        </Grid>

        <TextBlock Grid.Row="2" Text="— Connection Settings —" Foreground="{StaticResource BrushTextDim}" Margin="0,0,0,10" />
        <StackPanel Grid.Row="3" Margin="0,0,0,20">
            <Grid Margin="0,0,0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Port (TCP):" VerticalAlignment="Center" />
                <TextBox x:Name="TxtTcpPort" Grid.Column="1" Text="7201" Background="{StaticResource BrushSurface}" Foreground="{StaticResource BrushText}" BorderThickness="0" Padding="5"/>
            </Grid>
            <Grid Margin="0,0,0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Secret Key:" VerticalAlignment="Center" />
                <TextBox x:Name="TxtSecret" Grid.Column="1" Text="default_secret" Background="{StaticResource BrushSurface}" Foreground="{StaticResource BrushText}" BorderThickness="0" Padding="5"/>
            </Grid>
            <StackPanel Orientation="Horizontal" Margin="0,15,0,0" HorizontalAlignment="Center">
                <Button x:Name="BtnConnect" Content="Start Connection" Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Left" Padding="15,8" Margin="0,0,10,0" Click="BtnConnect_Click" />
                <Button x:Name="BtnSettings" Content="Hotkey Settings" Background="{StaticResource BrushSurface}" HorizontalAlignment="Left" Padding="15,8" Margin="0,0,10,0" Click="BtnSettings_Click" />
                <Button x:Name="BtnExit" Content="Exit" Background="{StaticResource BrushError}" HorizontalAlignment="Left" Padding="15,8" Click="BtnExit_Click" />
            </StackPanel>
        </StackPanel>

    </Grid>
</Window>
