<Window x:Class="InputBridge.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="InputBridge Client" Height="400" Width="400"
        Style="{StaticResource DefaultWindowStyle}"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Status Panel -->
        <Border Grid.Row="0" Background="{StaticResource BrushSurface}" CornerRadius="8" Padding="15" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="🔴 Waiting for Connection..." Foreground="{StaticResource BrushError}" FontWeight="Bold" FontSize="16" x:Name="TxtStatus" />
                <TextBlock Text="Listening Port: 7201 (TCP/UDP)" Foreground="{StaticResource BrushTextDim}" Margin="0,5,0,0" x:Name="TxtDetails" />
            </StackPanel>
        </Border>

        <TextBlock Grid.Row="1" Text="— Connection Settings —" Foreground="{StaticResource BrushTextDim}" Margin="0,0,0,10" />
        <StackPanel Grid.Row="2" Margin="0,0,0,20">
            <Grid Margin="0,0,0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Secret Key:" VerticalAlignment="Center" />
                <TextBox x:Name="TxtSecret" Grid.Column="1" Text="default_secret" Background="{StaticResource BrushSurface}" Foreground="{StaticResource BrushText}" BorderThickness="0" Padding="5"/>
            </Grid>
            <StackPanel Orientation="Horizontal" Margin="0,15,0,0">
                <Button x:Name="BtnConnect" Content="Connect" Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Left" Padding="15,8" Margin="0,0,10,0" Click="BtnConnect_Click" />
                <Button x:Name="BtnHide" Content="Hide" Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Left" Padding="15,8" Margin="0,0,10,0" Click="BtnHide_Click" />
                <Button x:Name="BtnExit" Content="Exit" Background="{StaticResource BrushError}" HorizontalAlignment="Left" Padding="15,8" Click="BtnExit_Click" />
            </StackPanel>
        </StackPanel>

    </Grid>
</Window>
